name: safepulse
description: SafePulse Emergency Response Mobile App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^4.0.2
  geolocator: ^11.0.0
  dio: ^5.4.3
  flutter_secure_storage: ^9.0.0
  flutter_local_notifications: ^17.0.0
  wakelock_plus: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
